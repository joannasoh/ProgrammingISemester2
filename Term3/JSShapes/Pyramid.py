class Pyramid():
	
	def __init__(self,length, breadth, height, base):
		self.length = length
		self.breadth = breadth
		self.height = height
		self.base = length * breadth
	def getVolume(self):
		self.volume = ((length * breadth * height)/3)
		return volume 
	
