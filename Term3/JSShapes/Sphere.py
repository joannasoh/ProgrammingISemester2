class Sphere():
	def __init__(self,diameter,radius,pi):
		self.diameter = diameter
		self.radius = radius
		self.pi = math.pi
	def getVolume(self):
		self.volume = ((4 * math.pi * radius * radius)/3)
		return volume
	def getSurfaceArea(self):
		self.sArea = (4 * math.pi * radius * radius)
		return sArea
